import React, { memo } from 'react'
import { Text } from 'react-native'

const ModalItem = (props) => {
  return (
    <>
      <Text>Ini Modal Item</Text>
    </>
  )
}

export default memo(ModalItem);